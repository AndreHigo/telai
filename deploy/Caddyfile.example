{$DOMAIN} {
    log {
        output file /var/log/caddy/telai-access.log {
            roll_size 50MiB
            roll_keep 5
            roll_keep_for 168h
        }
        format json
    }

    @internalMetrics path /metrics
    respond @internalMetrics 404
    reverse_proxy mirante:8787
}
